# Release Signing (required for Play/App Store — NOT included in repo)
Android: create key.properties + .jks, set in android/app/build.gradle.
iOS: Apple Developer cert + provisioning profile + Xcode signing settings.
This repo contains NO secrets; set up locally / via CI secrets.
