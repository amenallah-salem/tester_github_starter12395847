# Backend — Django

Django + Django REST Framework API with PostgreSQL. See `backend/config`, `backend/apps`, `backend/tests`, `backend/seed_data`.

> **Scaffold placeholder.** This directory is expanded in the relevant phase (see `docs/architecture.md` and the root `README.md`).
