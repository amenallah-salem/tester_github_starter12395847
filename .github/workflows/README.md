# GitHub Actions workflows

Placeholder for the project's CI/CD workflows.

> **Scaffold placeholder.** The CI foundation (checkout, deps, lint, backend tests,
> Flutter tests, build validation; later Android/iOS release builds) is added in the
> dedicated CI tasks (see `docs/ci-cd.md`). No workflow is active here yet so the
> scaffolding phase does not fail on empty backend/app projects.
