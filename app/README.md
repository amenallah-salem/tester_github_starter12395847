# App — Flutter application

Flutter client (Android, iOS, Web). Features live in `lib/features/`; shared code in `lib/core`, `lib/config`, `lib/models`, `lib/services`, `lib/repositories`, `lib/state`.

> **Scaffold placeholder.** This directory is expanded in the relevant phase (see `docs/architecture.md` and the root `README.md`).
