# Dev override for frontend: hot reload instead of static build
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports: ["8080:80"]
    volumes: ["./frontend:/app"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
